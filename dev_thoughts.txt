## 28.03.2026

Пытаюсь урезать длину результатов поиска и контента веб-страниц. 
Последняя попытка просить сократить и выделить важное прямо в результате выполнения инструмента - безуспешно. Пишет "сейчас выделю главное" и запрашивает следующую страницу >.<'

Не получается добиться этого ни через описание функции, ни через системный промпт

Добавляю второго агента, который будет возвращать сокращенный результат



## 30.03.2026

Сделал, чтобы функция `read_file` возвращала оглавление файла и содержимое указанной секции. 
Не учел, что заголовки разного уровня теперь определяют границы секций как равноправные (



## 31.03.2026

Хочу сделать сменяемые системные промпты через параметры командной строки. Сам промпт вынести в файл, в недоступное самому агенту место. 
Попробовать промпты покороче. 

Надо сделать более удобный интерфейс ввода сообщений, а то это какое-то мучение. Ввод в питоновском input редактируется только backspace'ом ><
Например сделать бота в телеграме... Скорее всего нужно хостить какой-то endpoint, чтобы через него пропускать запросы агента и telegram..



## 04.04.2026

Агент не видит сам и не сохраняет никак свое финальное сообщение. Хотя я сам в системных промптах указываю, что это сообщение - самое важное!
Там включена исходная проблема, действия и выводы. А я никак их не сохранияю. Я забыл о соей изначальной идее.
После выполнения задания у бота остается только сжатый лог выполнения, но нет выводов и финального наводящего вопроса.

В main.py хочу сделать model_run() и внутрь поместить цикл выполнения.

Сделать индексирование памяти тегами, ассоциативным рядом понятий, которые имеют отношение к записям в большой памяти.
Чтобы бот никогда не читал файл памяти целиком!



## 05.04.2026

Большие файлы теперь читаются реже. Разметка тегами это интересно, сделаю потом.
Буду размечать тегами накопительную историю из 'compressed_history.txt' и 'completed_tasks_histroy.txt' (состоит из финальных ответов)
Пока пускай копится база знаний.

Такими тегами можно разметить разнообразную документацию библиотек или API.



## 05.04.2026

Для чего использовать агента?
Анализировать рынок на предмет повторяющихся паттернов?



## 05.04.2026

Дичайшая проблема безопасности в том, что агент может запросто узнать полный путь рабочей директории через самописный python-скрипт и пойти лазить по этому пути куда угодно в системе...
Поэтому агенты должны быть облачными и сидеть в сандбоксе с ограниченными правами пользователя.
Самописные скрипты - вторая дичайшая проблема безопасности.

Сделал возможность ему писать приложения, но он же не сможет следить за его выполнением асинхронно, получать результаты в течение работы такого приложения...

Теперь еще и доглосрочная память на китайском, чтобы токены экономить, но хрен поймешь какой там мусор копится...

Что-то замечаю, что экономия токенов не особо сказалась на стоимости за токен. Токенов используется действительно меньше, но они стали дороже. Зато стоимость за API вызов стала меньше.
Пускай лучше контекст остается дольше и разрастается, чем частая компрессия - нарушается префикс чата и модель влетает в cache miss.



## 06.04.2026

Подтвердил токенайзером, что китайский язык гораздо экономнее. core_system_prompt.md на китайском использует почти в 4 раза меньше токенов, чем на английском. 145 против 542. Меньше в 3.73 раза
Промпт компрессора вообще уменьшается больше чем в 5 раз благодаря иероглифам. С 572 до 112. Разница в 5.11 раз

Подумал, что можно решить проблему с необходимостью перезагрузки после исправления инструментов ботом самому себе сделав каждый инструмент отдельным внешним скриптом. Бот показал мне как это реализовать в питоне.
Кстати и добавление новых тоже не проблема! Добавление инструментов не требует менять никаких начальных настроек!
Каждый запрос независим, и список инструментов передается с каждым запросом. Ничто не мешает вписать туда еще один в любой момент

Выпилил у него инструмент проверки памяти - бесполезная процедура, написанная самим ботом. Заботиться ему о сохранении целостности своей памяти не целесообразно. Тем более, что залезть в память и что-нибудь понять дико проблематично, когда она на китайском. 
Выпилил узнавание даты. Она вписана в системный промпт.
Сохранение токенов больше не актуально, а то он и так похоже дважды прошелся по системным промптам, уж сильно коротко описан каждый пункт. Да и инструмент оценки чисто формальный, и построен на ошибочном выводе, что DeepSeek считает токены по символам. Текущее потребление он оценить не может всё равно. Выпилил инструмент.

Можно поработать над системой памяти. Не заставлять считывать файлы по секциям, а иметь много небольших файлов.
Не один большой модуль с расширениями, а отдельные файлы скриптов.
Не один большой файл памяти, а несколько с разной степенью подробности:
файл с историей задач:
 # [время][описание сессии в одну строку (заголовок)]
 # [время][описание сессии в одну строку (заголовок)] 
 ...

файл с сжатыми контекстами с указанием временных промежутков между сжатиями:
 # [время - время]
 [абзац со сжатой историей]
 
 # [время - время]
 [абзац со сжатой историей]
 ...

файл с результатами сессии по одному абзацу на каждую (с финальными сообщениями, которые должны обеспечивать репродуктивность результата сессии):
 # [время][описание сессии в одну строку (заголовок)]
 [запрос пользователя]
 [действие][параметры (цель)][результат (ссылка, факт)]
 [действие][параметры (цель)][результат (ссылка, факт)]
 ...
 [Окончательный вывод]
 [Источники]
 # [время][описание сессии в одну строку (заголовок)]
 [запрос пользователя]
 [действие][параметры (цель)][результат (ссылка, факт)]
 [действие][параметры (цель)][результат (ссылка, факт)]
 ...
 [Окончательный вывод]
 [Источники]
 # [время][описание сессии в одну строку (заголовок)]
...



## 08.04.2026

Попросил удалить старые файлы, в том числе свой демо-проект, который он создавал, чтобы показать что умеет делать приложения теперь. Там была структура папок, которые он не смог бы удалить существующими инструментами.
Но он все равно удалил. Самописным python-скриптом. Писец, опасный тип!



# 09.04.2026

Использование второго агента на том же аккаунте ломает контекстный кеш. Надо заводить второй аккаунт ради агента-компрессора?..
Добавить таймаут операциям, чтоб не зависал, если побочный процесс зависнет

Сделать sample_script.py чтобы он считал стиль кода перед тем как писать свой код. Понял как делить большой код на секции.
Снова и снова пытается писать большой файл полностью. Когда видит ошибку переполнения длины вывода, тогда использует append_file. Получает скрипт с ошибками.
Запускает скрипт, обнаруживаются синтаксические ошибки. Пытается исправить с помощью edit_file_lines - не выходит у него. Может получает сообщение об ошибке с номером строки, а при считывании файла построчно нумерация не совпадает.
Тогда создает новый скрипт попроще.. На все эти шаги уходят токены!

Полнейший рукожоп он, а не вайбкодер!

extended_tools_guidelines.md уже дико разросся. Наверное пора выделять отдельного агента-кодера.





